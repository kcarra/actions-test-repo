name: sleep then echo

on:
  push:

jobs:
  sleep-echo:
    runs-on: ubuntu-latest
    steps:
      - name: sleep then echo
        run: |
          echo "Sleeping for 1000..."
          sleep 1000
          echo "Time to wake up!"
          exit 0